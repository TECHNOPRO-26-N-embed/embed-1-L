# 基本設計書 — 組込み開発実習

<!-- 作成者: 堀航成 / 日付: 2026-05-22 / グループ: 1-L -->
<!-- ⏱️ 目安時間: 2時間（作成）+ 30分（AIレビュー）+ 30分（グループレビュー・修正） -->

> **このドキュメントの目的**  
> 要件定義書（requirements.md）で「何を作るか」を定義しました。  
> この基本設計書では「**どのような構造で作るか**」を決めます。  
> **コードは書きません**。「どう繋ぐか・どう分割するか・どう動かすか」の設計図です。

> [!NOTE]
> 詳細設計書（detailed_design.md）では「各処理を具体的にどう実装するか」を書きます。  
> 基本設計書は詳細設計書の**土台**になります。ここで決めることが多いほど、詳細設計が楽になります。

---

## 0. 要件定義書との接続確認

> ※ まず要件定義書（requirements.md）の内容を転記して確認します。

| 項目 | 内容（requirements.md から転記） |
|:--|:--|
| 作品タイトル | MemoFlash |
| 一言説明 | ランダムに光るLEDの順番を記憶して再現するゲーム |
| 必須機能（3-1）の件数 | 9件 |
| 使用する共通部品 | LCD1602 Module |
| 追加部品（主なもの） | LED4色(赤・黄・青・緑) / メンブレンキーバッド / パッシブブザー / ポテンショメーター |

> [!IMPORTANT]
> 要件定義書の「やらないこと（3-3）」に書いたことは、この設計書にも登場させないでください。

---

## 1. システム全体像

### 1-1. ブロック図（入力 → 処理 → 出力）

> ※ Arduinoを中心に、「何が入力で何が出力か」を整理します。  
> ASCII図または別紙の写真を貼り付けてください。

**記入例：**
```
【入力】               【Arduino UNO R3】          【出力】
超音波センサー ────→┐                       ┌──→ LED（赤・緑）
タクトスイッチ ────→┤    状態管理・制御      ├──→ パッシブブザー
                    └─────────────────────┘
```

**↓ 自分のシステムを書いてください**
```
【入力】                       【Arduino UNO R3】                     【出力】
メンブレンキーパッド（A/B/C/D） ───→┌──────────────────────────────┐──→ LED赤
                                  │ ・状態管理                    │──→ LED黄
                                  │ ・パターン生成（乱数）         │──→ LED青
                                  │ ・入力との正誤判定             │──→ LED緑
                                  │ ・ラウンド/スコア管理          │──→ LCD1602（ラウンド／ハイスコア表示）
                                  └──────────────────────────────┘──→ パッシブブザー（任意機能）
```

---

### 1-2. 状態遷移図（システムの「ふるまい」）

> ※ ガジェットが「どんな状態を持つか」を定義します。  
> 状態（○）と、状態が切り替わる条件（矢印の横）を書いてください。

**記入例：**
```
[電源ON]
  ↓ 初期化完了
[待機中] ─── センサー検知 ───→ [動作中] ─── 終了条件 ───→ [完了]
  ↑                                 │
  └──────── リセット ────────────────┘
```

**↓ 自分の状態遷移を書いてください**
```
[電源ON]
  ↓ 初期化完了

[待機中(TITLE)] ──── #キー押下 ────→ [パターン提示中(SHOW)] ──── 表示完了 ────→ [入力待ち(INPUT)]
     ↑                                        ↑                                     │
     │ #キー押下                               │ パターン最後まで正解                  │ 誤入力 / (任意)時間超過
     │ (ラウンド長を3にリセット)                │ ラウンド長 +1                        │
     │                                        └─────────────────────────────────────┤
     │                                                                              ↓
     └──────────────────────────────── [ゲームオーバー(OVER)] ←──────────────────────┘
```

【補足】
- INPUT状態は、押されたキーが、その時点で期待している色と一致しているかを1回ずつ判定する
- 入力が正解 かつ まだ残りの入力がある : INPUT状態のまま、次のキー入力を待つ
- 入力が正解 かつ 現在ラウンドの最後まで入力完了 : → ラウンド数を+1し、SHOW状態へ戻る
- 不正解: → OVER状態へ移る
- SHOW状態でのキー入力は完全に無視
- OVER状態は #キー以外を押しても反応しない
- OVER状態で # を押すと TITLE へ戻り、再度 # でゲーム開始となる

**状態の意味を説明してください：**

| 状態名 | この状態のとき、何をしているか |
|:--|:--|
| 待機中 (TITLE) | LCDに「Press # to START」を表示し、全LED消灯。#キー押下を待つ。押下時にラウンド長=3で初期化してSHOWへ遷移。 |
| パターン提示中 (SHOW) | 現ラウンド長分のランダムLEDパターンを生成し、順番に点灯/消灯して提示。キー入力は完全に無視。表示完了でINPUTへ遷移。 |
| 入力待ち (INPUT) | A/B/C/Dキー入力を1手ごとに受け付けて即時判定。一致なら次の手へ（自己ループ）。最終手まで正解なら次ラウンドとしてSHOWへ。不一致または時間超過でOVERへ。 |
| ゲームオーバー (OVER) | LCDに「Game Over / Round: X / Best: Y」を表示。不正解音をブザー出力。#キー押下を待ち、押下時にラウンド長=3へリセットしてTITLEへ戻る。 |

【補足】

- #キーの役割: TITLE/OVERではSTART/RESTARTに使用。INPUT状態で # を押された場合、 INPUT状態の # は未定義キーとして無効入力扱いとし、入力待ちを継続する（ゲームオーバーにはしない）

---

## 2. ハードウェア設計

### 2-1. 部品一覧と接続ピン

> ※ 使う部品とArduinoのどのピンに繋ぐかを決めます。  
> デジタル（D2〜D13）/ アナログ（A0〜A5）/ 通信（I2C: A4/A5, SPI: D10〜D13）

| 部品名 | 型番・種類 | Arduinoピン | 入力/出力 | 備考 |
|:--|:--|:--|:--|:--|
| LCD RS | LCD1602 | D7 | 出力 | LiquidCrystal ライブラリ使用 |
| LCD EN | LCD1602 | D8 | 出力 | 〃 |
| LCD D4 | LCD1602 | D9 | 出力 | 〃 |
| LCD D5 | LCD1602 | D10 | 出力 | 〃 |
| LCD D6 | LCD1602 | D11 | 出力 | 〃 |
| LCD D7 | LCD1602 | D12 | 出力 | 〃 |
| LED 赤 | 5mm LED | D2 | 出力 | 220Ω 抵抗と直列 |
| LED 黄 | 5mm LED | D4 | 出力 | 220Ω 抵抗と直列 |
| LED 青 | 5mm LED | D5 | 出力 | 220Ω 抵抗と直列 |
| LED 緑 | 5mm LED | D6 | 出力 | 220Ω 抵抗と直列 |
| パッシブブザー | パッシブブザー | D3 | 出力 | tone() 関数使用 |
| キーパッド Row0 | 4×4 メンブレン | A0 | 入力 | INPUT_PULLUP。A キー検出用 |
| キーパッド Row1 | 4×4 メンブレン | A1 | 入力 | INPUT_PULLUP。B キー検出用 |
| キーパッド Row2 | 4×4 メンブレン | A2 | 入力 | INPUT_PULLUP。C キー検出用 |
| キーパッド Row3 | 4×4 メンブレン | A3 | 入力 | INPUT_PULLUP。D / # キー検出用 |
| キーパッド Col2（#） | 4×4 メンブレン | A4 | 出力 | # キーが属する列 |
| キーパッド Col3（ABCD） | 4×4 メンブレン | A5 | 出力 | A/B/C/D キーが属する列 |

> [!TIP]
> **ピン競合チェック：** I2C（A4/A5）・SPI（D10〜D13）は複数部品で共用します。  
> デジタルピン（D2〜D9）は原則1部品1ピンです。D0/D1はシリアル通信に使われるため避けること。

### 2-2. 使用する抵抗・回路メモ

> ※ 抵抗値の計算や、注意が必要な接続を記録してください。

| 用途 | 計算・メモ |
|:--|:--|
| LED 電流制限抵抗 | (5V − 2.0V) ÷ 20mA ≒ 150Ω →  220Ω を使用（安全側） |
| キーパッド プルアップ | INPUT_PULLUP（内蔵 20kΩ プルアップを使用、外付け抵抗不要） |
| LCD VO（コントラスト） | ポテンショメータで分圧（Arduino ピン不使用） |
| LCD R/W | GND に直結（Arduino ピン不使用） |
| ブザー | パッシブブザーは GND 直結。tone() で矩形波を生成 |

---

## 3. ソフトウェア構造設計

### 3-1. 使用するライブラリ

> ※ 使うArduinoライブラリを調べておきます。

| ライブラリ名 | 用途 | インストール方法 |
|:--|:--|:--|
| LiquidCrystal.h | LCD1602 の表示制御 | Copy Me First/LiquidCrystal/ からコピー済み |
| （追加ライブラリなし） | キーパッドは自前スキャン関数で実装。<br>tone() / millis() / random() は Arduino 組み込み関数 | — |
| Keypad.h | （参考）キット付属サンプル（custom_keypad.ino）は使用。<br>ただし 8ピン必要でピン不足のため、本作では不採用。<br>代わりに自前 6ピンスキャン関数（scanKeypad）で実装。 | Copy Me First/Keypad.zip |

### 3-2. 関数一覧（役割の定義）

> ※ プログラムを小さな「処理の部品（関数）」に分割します。  
> **ここではコードを書かず、「何をする関数か」だけ日本語で書いてください。**  
> 具体的な実装は詳細設計書（detailed_design.md）で行います。

| 関数名 | 何をする関数か（1〜2行で） | 引数（入力） | 戻り値（出力） |
|:--|:--|:--|:--|
| `setup()` | ピンモード設定・LCD初期化・乱数シード設定 | なし | なし |
| `loop()` | gameState を見て各状態関数を呼び出すメインループ | なし | なし |
| `initGame()` | ラウンド長を3に初期化。inputCount をリセット | なし | なし |
| `showTitle()` | LCDに「Press # to START」を表示。#キー待ち。押されたら SHOW へ遷移 | なし | なし |
| `generatePattern(length)` | 指定した長さ分のランダムな色（0〜3）を pattern[] 配列に生成 | int length | なし |
| `showPattern()` | pattern[] の順番に LED を1つずつ点灯/消灯。完了後 INPUT へ遷移 | なし | なし |
| `waitInput()` | キースキャンを行い、入力が pattern[inputCount] と一致か判定。正解なら継続/完走で SHOW へ、不正解で OVER へ遷移 | なし | なし |
| `showGameOver()` | LCDに最終ラウンド・ハイスコアを表示。ブザーで不正解音。#キー待ち。押されたら TITLE へ遷移 | なし | なし |
| `scanKeypad()` | 6ピンカスタムスキャンでキー入力を読み取る | なし | char（'A'〜'D'・'#'・'\0'=未入力） |
| `lightLED(color)` | 指定した色（0=赤/1=黄/2=青/3=緑）のLEDを点灯 | int color | なし |
| `allLEDOff()` | 4色のLEDを全消灯 | なし | なし |
| `updateLCD(round, best)` | LCDの1行目にラウンド数、2行目にハイスコアを表示 | int round, int best | なし |
| `playSound(type)` | type=1で正解音・type=0で不正解音をブザーで鳴らす（任意機能） | int type | なし |

### 3-3. タイミング設計（delay vs millis）

> ※ 複数の処理を同時に動かす場合、`delay()`を使うと他の処理が止まります。  
> 各タイマーが `delay` か `millis` かを事前に決めておきます。

| 処理 | 周期 | delay / millis | 理由 |
|:--|:--|:--|:--|
| タイトル画面のキー監視（showTitle） | 10msごと | **millis** | #キー待ち中も処理を止めず、即応性を確保するため |
| キーパッドスキャン（scanKeypad） | 5〜10msごと | **millis** | 入力取りこぼしを防ぎ、1手ごとの即時判定を行うため |
| デバウンス判定 | 30ms | **millis** | チャタリングによる誤入力を抑えるため |
| パターン提示: LED点灯時間（showPattern） | 400ms/手 | **delay** | SHOW状態ではキー入力を無視する仕様で、単純な順次表示を優先するため |
| パターン提示: 消灯間隔（showPattern） | 200ms/手 | **delay** | 点灯の区切りを明確にして視認性を上げるため |
| 入力タイムアウト判定（任意） | 3000ms/手 | **millis** | 時間超過を監視しつつ他処理を止めないため |
| LCD更新（updateLCD） | 状態遷移時のみ | **millis** | 毎ループ更新を避け、表示ちらつきと無駄な処理を減らすため |
| ブザー鳴動（playSound, 任意） | 正解100ms / 不正解500ms | **millis** | 音を鳴らしながら状態更新・表示更新を継続できるようにするため |

---

## 4. 異常系・エラー処理の方針

> ※ 要件定義書の「非機能要件（3-4）」を具体化します。  
> 詳細な実装方法は詳細設計書で書きます。ここでは「どう検知し、どう対応するか」の方針だけ。

| 異常ケース | 検知方法 | 対応方針 |
|:--|:--|:--|
| チャタリング（キーパッド） | 前回の有効入力から 50ms 以内に次の入力を検出した場合 | millis() で経過時間を管理。50ms 未満の入力は無効として破棄 |
| 同時押し（複数キー） | 6ピンスキャン中に複数の Row が同時に LOW になる場合 | その読み取りを無効として破棄。直前の状態を維持する |
| SHOW 状態でのキー入力 | gameState == SHOW のとき | スキャンは行うが結果を無視。状態遷移を発生させない |
| INPUT 状態での未定義キー（# など） | scanKeypad() の戻り値が A/B/C/D 以外 | 無効入力として破棄。誤入力扱いにはせず入力待ちを継続 |
| OVER 状態での # 以外のキー | gameState == OVER かつ '#' 以外を検出 | 無視。# キーのみ TITLE 遷移のトリガとして受け付ける |
| SHOW→INPUT 遷移時のキー持ち越し  | SHOW終了直後に押下が残っている可能性がある場合 | 遷移時に scanKeypad() を1回空読みして入力バッファをクリアする |
| 同時押し検出時のスキャン処理      | 6ピンスキャンで複数Rowが同時にLOWになった場合  | そのスキャンサイクル全体を無効とし、次のサイクルまで待つ（1サイクル無効化）|

---

## 5. 実装計画（タスクの優先順位）

> ※ 基本設計書が完成したら、どの順番で実装するかを決めます。

| 優先度 | 実装内容 | 対応機能 | 完了 |
|:--|:--|:--|:--|
| 1 | 配線完了 + LED 1個を点灯確認（D2 赤LED から始める） | 全機能の前提 | [ ] |
| 2 | LCD1602 動作確認（HelloWorld.ino を流用） | 共通部品の動作確認 | [ ] |
| 3 | LED 4色 + ブザーの単体テスト（順番に光らせる・鳴らす） | 出力系の確認 | [ ] |
| 4 | scanKeypad() の実装 + シリアルモニタで押下確認 | 入力系の確認 | [ ] |
| 5 | generatePattern() + showPattern() の実装 | 3-1①⑧⑨（パターン生成と提示） | [ ] |
| 6 | waitInput() + 正誤判定の実装 | 3-1②③④⑤（入力受付と判定） | [ ] |
| 7 | 状態遷移の全体統合 + LCD 表示（updateLCD） | 3-1⑥⑦（画面表示・ゲームフロー） | [ ] |
| 8 | 異常系の実装（デバウンス・同時押し対応） | 非機能要件 3-4 | [ ] |
| 9 | 任意機能（ブザー音 / 速度上昇 / 入力制限時間） | 3-2 | [ ] |

---

## 6. AIレビュー記録（基本設計版）

### Q1: 構造の妥当性確認
> 「この基本設計書をもとに Arduino UNO R3 でコードを書くとき、  
> ピン不足・ライブラリの競合・状態設計の漏れはありますか？」

**AIの回答（要約）：**

- **結論**: UNO R3 の現行構成で、致命的なピン不足と明確なライブラリ競合はない。
- **ピン設計**: D2〜D12 と A0〜A5 の割当で成立。D0/D1 未使用も適切。4×4キーパッドを A/B/C/D/# に限定した 6 ピンスキャン方針は妥当。
- **競合リスク（将来拡張時）**: A4/A5 をキーパッド列に使用しているため I2C 追加時は再配線が必要。D10〜D12 を LCD で使用しているため SPI 追加時も競合の可能性あり。
- **状態設計の要修正点**: 「INPUT で # を誤入力扱い」と「INPUT で # などを無効入力として継続」が文書内で不一致。どちらかに仕様統一が必要。
- **実装前の明確化推奨**: OVER から # 押下時の遷移（TITLE 経由で再開するか）と、SHOW→INPUT 遷移直後のキー持ち越し防止（入力バッファ初期化）を詳細設計で明記すると実装が安定する。

**対応した内容：**

- 1-2 の補足に「INPUT中の # は誤入力（ゲームオーバー）」と記述していたが、要件定義書 3-4「未定義値は無効入力として扱い判定に使わない」と矛盾していた。4章の記述（無効入力として破棄・継続）が正しいため、1-2 の補足を修正した。
- OVER → TITLE → SHOW の遷移で # を2回押す運用は意図的な設計。現状維持とし、1-2 の補足にその旨を追記した。
- 4章の異常系に「遷移時に scanKeypad() を1回空読みしてバッファをクリアする」を追記した。
- 4章の同時押し対応の記述を「そのスキャンサイクル全体を無効とし、次のサイクルまで待つ」とより具体的に修正した。

---

### Q2: 詳細設計への引き継ぎ確認
> 「この基本設計書から詳細設計に進むとき、  
> まだ決まっていない・詳細化が必要な部分はどこですか？」

**AIの回答（要約）：**

**対応した内容：**

---

## 7. グループレビュー記録

### 7-1. 指摘一覧

| No | 指摘内容 | 対応 |
|:---|:---|:---|
| 1 |  |  |
| 2 |  |  |
| 3 |  |  |

### 7-2. レビューを受けて変更した点

- 

---

*初版: 2026-05-22 / グループレビュー後更新: 2026-05-22*
